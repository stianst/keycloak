## TODOs

* Add some tests for the resource version generation
* Test with custom theme and resource version
* Test with development (theme edited in themes) 
  - Check gzip cache disabling
  - Check theme cache disabling
  - Check client cache disabling

  - Maybe make it easier to do development?
  - Maybe reload theme automatically if theme.properties file is updated